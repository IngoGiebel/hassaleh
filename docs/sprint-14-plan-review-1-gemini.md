# Sprint-14 Round-1 Plan Review — gemini-reviewer (Nisaba)

Reviewer: Nisaba 🌾 (`worker-codex`, acting in the requested `gemini-reviewer` clarity / consistency role)
Date: 2026-05-04
Scope: `docs/sprint-14-plan.md` v0 on trunk; checked against `docs/sprint-13-plan.md`, `docs/sprint-13-track-d-review.md`, `docs/sprint-13-track-d-review-round2.md`, `docs/sprint-13-track-d-implement.md`, `docs/sprint-14-backlog.md`, and `sprint-14-state.json`.

## Findings

| Type | Pointer | Finding |
|---|---|---|
| **BLOCKING** | `docs/sprint-14-plan.md:159-172` vs `docs/sprint-13-track-d-review.md:160-168` | **Track G's seven-advisory table is not verbatim and contains at least one materially wrong source drift.** The Sprint-13 source §6 enumerates seven advisories without `D-A*` IDs or `P1/P2` severity tags, and its #7 says `_LOGGER_NAME = "hassaleh.runtime"` is **not** in `SERVICE_ENUM` and that this is correct because logger name and service enum are different surfaces. Sprint-14 §5.G rewrites #7 as "Confirm `_LOGGER_NAME = "hassaleh-daemon"` matches the structlog `service` enum", which inverts the source and changes the literal logger name. That would send Track G to implement the wrong fix. |
| **ADVISORY** | `docs/sprint-14-plan.md:166-172` vs `docs/sprint-13-track-d-review.md:162-168` | **Fix-sketch wording is editorial, not source-verbatim.** Several sketches are reasonable implementation choices, but they are not lifted verbatim from the source: #2 source suggests `_, _ = _ensure_metrics(); return _registry` or returning the registry from `_ensure_metrics()`, while Sprint-14 prescribes `if _registry is None: raise RuntimeError(...)`; #3 source frames the collision as a Track-E `/metrics` exposure reconciliation, while Sprint-14 prescribes adding a constant `sprint=` label or renaming the family; #6 source asks to surface the setup assumption in Track-A wire-up review, while Sprint-14 prescribes a module-docstring + setup-then-emit integration test. If these are Dione's chosen v1.0.1 resolutions, label them as proposed resolutions rather than claiming verbatim inheritance. |
| **ADVISORY** | `docs/sprint-14-plan.md:155`, `docs/sprint-14-plan.md:189-191`, `docs/sprint-14-plan.md:222-224` | **Track G file-size framing is internally inconsistent.** The track table lists `src/hassaleh/runtime/observability.py`, `src/hassaleh/obs/tracing.py`, `tests/test_runtime_observability.py`, and `docs/sprint-13-plan.md`; §5.x says G touches only `observability.py`; §6.1 says "one file, six tests". For a bounded re-trial decision (S14-OQ-1), this matters: the plan should describe the same expected diff surface everywhere. |
| **OBSERVATION** | `docs/sprint-14-plan.md:9-17`, `docs/sprint-14-plan.md:128-132`, `docs/sprint-14-plan.md:352-366` | **The Δ-baseline inheritance rule is understandable to a fresh reader.** It states the authoritative inherited sections (§1 Requirements, §2 Architecture, §3 Interface Specification), says not to re-litigate them, and gives routing guidance for any inherited-section defects (Sprint-13-plan v2). No ambiguity found in the anti-scope guard. |
| **OBSERVATION** | `docs/sprint-14-plan.md:37-48`, `docs/sprint-14-plan.md:149-155`, `docs/sprint-14-plan.md:205-227` | **§0 / §5 / §6 are directionally consistent on worker reassignment.** §0 says the four pending tracks moved away from worker-codex/worker-gemini; §5 assigns B/C/E/G to worker-opus and F to dione-main; §6 keeps codex/gemini unused by default while raising S14-OQ-1 as a review question. The only correction needed is the Track-G diff-surface wording above. |
| **OBSERVATION** | `docs/sprint-14-plan.md:222-227`, `sprint-14-state.json` advisory `S14-OQ-1` | **S14-OQ-1 is fairly framed enough for review.** The default is explicit (hold only-opus), the rationale is stated separately, and reviewers are invited to recommend revision. The phrase "small enough" depends on fixing the Track-G diff-surface inconsistency, but I do not read the question as leading. |
| **OBSERVATION** | `docs/sprint-14-plan.md` SHA refs `af6803b`, `1499117`, `c0b04f1`; Sprint-13 section anchors | **Cross-references checked.** All three SHA references resolve in trunk history. Sprint-13 section references used by Sprint-14 are reachable: §1, §2, §2.6, §3, §3.1-§3.6, §5, §6.5, §7.1, §7.2, §8, and §9 all exist in `docs/sprint-13-plan.md`. |

## Overall verdict

**CHANGE-REQ**

Required before CLEAN: rewrite §5.G so the seven advisory rows either (a) quote the Sprint-13 Track-D §6 source faithfully and keep Dione's chosen fixes in a separate "Sprint-14 proposed resolution" column, or (b) explicitly state that the table is a normalized remediation table rather than a verbatim lift. In either case, correct D-A7's logger/service wording: source logger name is `_LOGGER_NAME = "hassaleh.runtime"`, and the source point is that it is intentionally not a `SERVICE_ENUM` value.

Verification performed: git-history SHA resolution via `git cat-file -t`, Sprint-13 heading reachability by line inspection, and direct text comparison of Sprint-14 §5.G against Sprint-13 Track-D review §6 and Round-2 deferred-advisory table. No test suite run; this is a plan/document review only.
